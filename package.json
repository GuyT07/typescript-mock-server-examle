{
  "name": "typescript-mock-server-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run --prefix node_modules/typescript-mock-server start -- --path=$INIT_CWD/tms-models --port=3000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript-mock-server": "https://github.com/GuyT07/typescript-mock-server"
  }
}
